@model IBoard

@{
    ViewBag.Title = "Удаление доски";
    Layout = "_Layout";
}

<h1>@ViewBag.Title</h1>

<hr />
<div class="container">
    <div class="row">
        <div class="col-sm">
            <h2>Вы точно хотите удалить @Model.Name?</h2>
            <hr />
            
            <form asp-action="Remove" method="Post">
                <input type="hidden" name="boardId" asp-for="Id" />
                <input type="submit" value="Удалить" class="btn btn-default"/>
                <a asp-action="Threads" asp-controller="Thread" asp-route-boardId="@Model" class="btn btn-default">Отмена</a>
            </form>
        </div>
    </div>
</div>
